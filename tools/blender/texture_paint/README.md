# Phase 56: hand-painted albedo textures pipeline
# Source .blend with vertex colors, export albedo 1024x1024 PNG
# hero_green and hero_leaf start
# Workflow: Substance Painter / Blender Texture Paint -> bake -> downsample to atlas
# Albedo stored in android/assets/generated/textures/
# Fallback to procedural PALETTE if missing
