package com.amirrezahadipoor.herodefense.model;

/** Gameplay-selected clip names matching the Blender Hero atlas — E2 adds WALK. */
public enum HeroAnimationState {
    IDLE,
    WALK,
    ATTACK,
    HIT,
    DEATH
}
